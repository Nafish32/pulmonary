# Quick-but-real probe: 5000-patient random slice, 20 epochs. Enough to judge
# whether the methodology is worth pursuing -- NOT thesis-grade numbers.
fast_mode: true
epochs: 20
imgsz: 512
batch: 8
png_size: 640
max_patients: 5000   # bump toward full RSNA (~26k) as you trust the path; None = all
